import React from "react";
import LoginForm from "@/components/Login/LoginForm";

const LoginPage = () => {
  return (
    <main className="flex justify-center items-center h-screen overflow-auto">
      <LoginForm />
    </main>
  );
};

export default LoginPage;
